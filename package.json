{
  "name": "cutout-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"pnpm dev:backend\" \"pnpm dev:frontend\"",
    "dev:backend": "python -m backend.cutout_studio.server",
    "dev:frontend": "vite --host 127.0.0.1",
    "build": "tsc && vite build",
    "test": "python -m unittest discover -s backend/tests -p \"test_*.py\" && node --experimental-strip-types tests/detailEditor.test.ts && node --experimental-strip-types tests/traceWorkflow.test.ts && node --experimental-strip-types tests/traceStrokes.test.ts && node --experimental-strip-types tests/traceViewport.test.ts && node --experimental-strip-types tests/cutoutProject.test.ts",
    "verify": "pnpm test && pnpm build"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^9.1.2",
    "lucide-react": "^0.468.0",
    "vite": "^6.0.5",
    "typescript": "^5.7.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2"
  }
}
